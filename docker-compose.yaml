services:
  app:
    container_name: arch-fastapi
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: ["fastapi", "dev", "--host", "0.0.0.0", "--port", "8000"]
